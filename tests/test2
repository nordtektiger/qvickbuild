var1 = "foo_bar_baz";
test = var1: "*_bar_*" -> "*-quickbuild-*";
ans = "foo-quickbuild-baz";

"verify" {
  # automatically fail build if strings don't match by utilizing shell scripting.
  run = "if \[ '[test]' = '[ans]' \]; then exit 0; else exit -1; fi";
}


# --- tests more advanced replacement operator usage, as well as the declaration
#     of variables on line 1 which has been problematic in the past due to lexer
#     inaccuracies during initialization.
