# --- tests asterisk behaviour when used in both initialization and replace
"verify-3" {
  var1 = "./tests/*-1";
  test = ["./tests/test-1", "./tests/fault-1"]: var1 -> "success";
  ans = "success ./tests/fault-1";
  run = "if \[ '[test]' = '[ans]' \]; then exit 0; else exit -1; fi";
}
